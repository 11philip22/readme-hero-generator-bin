export function createPatternSeed(random = Math.random) {
  return {
    topoOffsetX: random() * 1800,
    topoOffsetY: random() * 1800,
    topoScaleX: 0.018 + random() * 0.012,
    topoScaleY: 0.03 + random() * 0.015,
    topoBandwidth: 0.018 + random() * 0.012,
    topoDotScale: 0.82 + random() * 0.55,
    voidOffsetA: random() * 1800,
    voidOffsetB: random() * 1800,
    voidOffsetC: random() * 1800,
    voidOffsetD: random() * 1800,
    voidWarpPrimaryX: 68 + random() * 40,
    voidWarpSecondaryX: 24 + random() * 26,
    voidWarpPrimaryY: 70 + random() * 42,
    voidWarpSecondaryY: 24 + random() * 26,
    voidLevels: 14 + Math.floor(random() * 6),
    voidLevelSpread: 1.3 + random() * 0.45,
    voidLineBoost: 0.88 + random() * 0.32,
    zebraOffsetX: random() * 1500,
    zebraOffsetY: random() * 1500,
    zebraStripeCount: 8.8 + random() * 3.6,
    zebraWarpFreqX: 0.005 + random() * 0.006,
    zebraWarpFreqY: 0.018 + random() * 0.013,
    zebraWarpAmp: 1 + random() * 1,
    zebraThreshold: 0.42 + random() * 0.16,
    zebraAlpha: 0.72 + random() * 0.16,
    zebraBrightnessShift: -0.02 + random() * 0.05,
  };
}
